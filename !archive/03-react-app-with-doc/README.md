# Product Management App

React-додаток для управління списком продуктів з функціоналом фільтрації, сортування та пагінації.

## 📋 Опис проекту

Повнофункціональний додаток для відображення та управління каталогом продуктів. Додаток демонструє роботу з великими списками даних, реалізацію складних фільтрів, сортування та пагінації в React з TypeScript.

## ✨ Функціональні можливості

### Основний функціонал

- ✅ Відображення списку продуктів у вигляді карток
- ✅ Детальна інформація про кожен продукт (назва, ціна, категорія, рейтинг, наявність на складі)
- ✅ Адаптивний дизайн для різних розмірів екранів

### Фільтрація

- 🔍 Пошук за назвою продукту (регістронезалежний)
- 📁 Фільтр за категорією (dropdown з усіма доступними категоріями)
- 💰 Фільтр за ціновим діапазоном (min/max)
- ⭐ Фільтр за рейтингом (від 1 до 5 зірок)
- 📦 Фільтр за наявністю на складі (checkbox)

### Сортування

- 🔤 За назвою (A-Z / Z-A)
- 💵 За ціною (зростання / спадання)
- ⭐ За рейтингом (найвищий / найнижчий)

### Пагінація

- 📄 Відображення продуктів сторінками (10/25/50 елементів на сторінку)
- ◀️ ▶️ Навігація між сторінками
- 📊 Відображення загальної кількості продуктів та поточної сторінки

### Додатковий функціонал

- 🧹 Кнопка скидання всіх фільтрів
- 🔄 Збереження стану фільтрів при перезавантаженні сторінки (localStorage)
- ⚡ Оптимізована продуктивність (React.memo, useMemo, useCallback)
- 🎨 Сучасний UI з hover-ефектами та плавними переходами

## 🛠 Технології

- **React 18** - UI бібліотека
- **TypeScript** - типізація
- **Vite** - збірка проекту
- **CSS3** - стилізація
- **ESLint** - лінтинг коду

## 📁 Структура проекту

```
src/
├── components/
│   ├── ProductCard/
│   │   ├── ProductCard.tsx
│   │   └── ProductCard.css
│   ├── ProductList/
│   │   ├── ProductList.tsx
│   │   └── ProductList.css
│   ├── Filters/
│   │   ├── Filters.tsx
│   │   └── Filters.css
│   ├── Sorting/
│   │   ├── Sorting.tsx
│   │   └── Sorting.css
│   └── Pagination/
│       ├── Pagination.tsx
│       └── Pagination.css
├── types/
│   └── product.ts
├── utils/
│   ├── filterProducts.ts
│   ├── sortProducts.ts
│   └── mockData.ts
├── hooks/
│   └── useLocalStorage.ts
├── App.tsx
├── App.css
├── main.tsx
└── index.css
```

## 🚀 Встановлення та запуск

### Встановлення залежностей

```bash
npm install
```

### Запуск у режимі розробки

```bash
npm run dev
```

Додаток буде доступний за адресою: `http://localhost:5173`

### Збірка для продакшену

```bash
npm run build
```

### Перегляд production збірки

```bash
npm run preview
```

## 💾 Типи даних

### Product Interface

```typescript
interface Product {
  id: number
  name: string
  price: number
  category: string
  rating: number
  inStock: boolean
  description?: string
  image?: string
}
```

### Filter State

```typescript
interface FilterState {
  searchQuery: string
  category: string
  minPrice: number
  maxPrice: number
  minRating: number
  inStockOnly: boolean
}
```

## 🎯 Основні компоненти

### ProductCard

Картка окремого продукту з відображенням всієї інформації та індикатором наявності.

### ProductList

Контейнер для відображення списку продуктів у вигляді grid-сітки.

### Filters

Панель фільтрації з усіма елементами управління фільтрами.

### Sorting

Компонент для вибору методу сортування продуктів.

### Pagination

Компонент пагінації з можливістю вибору кількості елементів на сторінку.

## 🔧 Кастомні хуки

### useLocalStorage

Хук для роботи з localStorage, забезпечує збереження та відновлення стану фільтрів.

## ⚡ Оптимізація

- Використання `React.memo` для уникнення непотрібних ре-рендерів
- `useMemo` для кешування відфільтрованих та відсортованих даних
- `useCallback` для мемоізації callback-функцій
- Ледача ініціалізація стану з localStorage

## 🎨 Дизайн

- Адаптивний grid layout (1-4 колонки залежно від ширини екрану)
- Картка продукту з hover-ефектами
- Індикатори наявності товару (зелений/червоний)
- Візуалізація рейтингу зірочками
- Чітка типографіка та spacing

## 📝 Примітки

Проект створено як навчальне завдання для демонстрації навичок роботи з React, TypeScript, управлінням стану, обробкою великих списків даних та оптимізацією продуктивності.

## 📄 Ліцензія

MIT
